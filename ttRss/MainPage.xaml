<Page
    x:Class="TinyTinyRSS.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TinyTinyRSS"
    xmlns:converter="using:TinyTinyRSS.Classes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <CollectionViewSource x:Name="groupedFeeds" IsSourceGrouped="True"/>
        <converter:IntToStringConverter x:Key="IntStringConverter" />
    </Page.Resources>

    <Grid>
        <!-- Adaptive triggers -->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="720" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="RootSplitView.DisplayMode" Value="CompactInline"/>
                        <Setter Target="RootSplitView.IsPaneOpen" Value="True"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="RootSplitView.DisplayMode" Value="CompactOverlay"/>
                        <Setter Target="RootSplitView.IsPaneOpen" Value="False"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <!-- Top-level navigation menu + app content -->
        <SplitView x:Name="RootSplitView"
                   DisplayMode="Inline"
                   OpenPaneLength="256"
                   IsTabStop="False">
            <SplitView.Pane>
                <ScrollViewer>
                    <StackPanel Margin="0,50,0,0">
                    <TextBlock Text="" x:Uid="SpecialFeeds" />
                    <ListView>
                        <ListViewItem>
                            <TextBlock  Name="All" Text="" x:Uid="AllFeedsTitle" Tapped="FeedTapped"/>
                        </ListViewItem>
                        <ListViewItem>
                            <TextBlock  Name="Fresh" Text="" x:Uid="FreshFeeds" Tapped="FeedTapped"/>
                        </ListViewItem>
                        <ListViewItem>
                            <TextBlock  Name="Starred" Text="" x:Uid="StarredFeeds" Tapped="FeedTapped"/>
                        </ListViewItem>
                        <ListViewItem>
                            <TextBlock  Name="Published" Text="" x:Uid="PublishedFeeds" Tapped="FeedTapped"/>
                        </ListViewItem>
                        <ListViewItem>
                            <TextBlock  Name="Archived" Text="" x:Uid="ArchivedFeeds" Tapped="FeedTapped"/>
                        </ListViewItem>
                        <ListViewItem>
                            <TextBlock  Name="Recent" Text="" x:Uid="RecentlyReadFeed" Tapped="FeedTapped"/>
                        </ListViewItem>                        
                    </ListView>
                    <SemanticZoom>
                        <SemanticZoom.ZoomedInView>
                            <ListView x:Name="AllFeedsList" 
                                        ItemsSource="{Binding Source={StaticResource groupedFeeds}}" 
                                        SelectionChanged="AllFeedsList_SelectionChanged"                                  
                                        Background="Transparent" VerticalContentAlignment="Top" ScrollViewer.HorizontalScrollBarVisibility="Auto" >
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid HorizontalAlignment="Stretch" Margin="0,0,0,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Image MaxWidth="24" MaxHeight="24" Source="{Binding feed.icon}" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" ScrollViewer.VerticalScrollBarVisibility="Disabled"/>
                                            <TextBlock TextWrapping="Wrap" HorizontalAlignment="Stretch" Text="{Binding feed.title}" FontSize="{StaticResource TextStyleMediumFontSize}" Margin="5" Grid.Column="1">
                                            </TextBlock>
                                            <TextBlock Text="{Binding feed.unread, Converter={StaticResource IntStringConverter}}" FontSize="{StaticResource TextStyleMediumFontSize}" Margin="0,5" Grid.Column="2" TextAlignment="Right" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    </Style>
                                </ListView.ItemContainerStyle>
                                <ListView.GroupStyle>
                                    <GroupStyle HidesIfEmpty="True">
                                        <GroupStyle.HeaderContainerStyle>
                                            <Style TargetType="ListViewHeaderItem">
                                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                            </Style>
                                        </GroupStyle.HeaderContainerStyle>
                                        <GroupStyle.HeaderTemplate>
                                            <DataTemplate>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text='{Binding Key.title}' Grid.Column="0"
                                           Foreground="{StaticResource PhoneAccentBrush}" FontSize="{StaticResource TextStyleMediumFontSize}" HorizontalAlignment="Stretch" />
                                                    <TextBlock Text='{Binding Key.combined}' Grid.Column="1" 
                                           Foreground="{StaticResource PhoneAccentBrush}" FontSize="{StaticResource TextStyleMediumFontSize}" HorizontalAlignment="Right" Margin="0,0,0,0"/>
                                                </Grid>
                                            </DataTemplate>
                                        </GroupStyle.HeaderTemplate>
                                    </GroupStyle>
                                </ListView.GroupStyle>
                            </ListView>
                        </SemanticZoom.ZoomedInView>
                        <SemanticZoom.ZoomedOutView>
                            <ListView Background="Black" ItemsSource="{Binding Source={StaticResource groupedFeeds}, Path=CollectionGroups}" Padding="25,25,0,0">
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                                        <Setter Property="VerticalContentAlignment" Value="Center" />
                                    </Style>
                                </ListView.ItemContainerStyle>
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text='{Binding Group.Key.title}' Grid.Column="0"
                                           Foreground="{StaticResource PhoneAccentBrush}" FontSize="{StaticResource TextStyleMediumFontSize}" />
                                            <TextBlock Text='{Binding Group.Key.combined}' Grid.Column="1" 
                                           Foreground="{StaticResource PhoneAccentBrush}" FontSize="{StaticResource TextStyleMediumFontSize}" Margin="0,0,0,0"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </SemanticZoom.ZoomedOutView>
                        </SemanticZoom>
                    </StackPanel>
                </ScrollViewer>
            </SplitView.Pane>
            
            <TextBlock Text="Content" />
        </SplitView>

        <!-- Declared last to have it rendered above everything else, but it needs to be the first item in the tab sequence. -->
        <ToggleButton x:Name="TogglePaneButton"
                      TabIndex="1"
                      Style="{StaticResource SplitViewTogglePaneButtonStyle}"
                      IsChecked="{Binding IsPaneOpen, ElementName=RootSplitView, Mode=TwoWay}"
                      Unchecked="TogglePaneButton_Checked"
                      AutomationProperties.Name="Menu"
                      ToolTipService.ToolTip="Menu" />
    </Grid>
</Page>
